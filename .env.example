# Copy to .env only when overriding local development defaults.
# .env is ignored by Git; never commit real credentials.
SENTINEL_DB_NAME=sentinel
SENTINEL_DB_USERNAME=sentinel
SENTINEL_DB_PASSWORD=sentinel
SENTINEL_POSTGRES_PORT=55432
SENTINEL_REDIS_PORT=6379
SENTINEL_REDIS_HOST=localhost
SENTINEL_RABBITMQ_HOST=localhost
SENTINEL_RABBITMQ_USERNAME=sentinel
SENTINEL_RABBITMQ_PASSWORD=sentinel
SENTINEL_RABBITMQ_PORT=5672
SENTINEL_RABBITMQ_MANAGEMENT_PORT=15672

# Phase 3 identity settings. Local secrets are generated automatically in
# ignored .sentinel/ storage by scripts/dev-env.ps1; never put real secrets here.
SENTINEL_JWT_ISSUER=sentinel-local
SENTINEL_JWT_AUDIENCE=sentinel-api
# Required secret names for external environments:
# SENTINEL_JWT_SECRET=<base64-encoded 32+ random bytes>
# SENTINEL_WEBHOOK_SECRET=<base64-encoded 32+ random bytes>

# Deployment safety: keep true until a reviewed environment passes its safety suite.
SENTINEL_REMEDIATION_DRY_RUN=true
SENTINEL_AGENT_ENABLED=false

# Enable only after deliberately installing/configuring the named models.
SENTINEL_AI_CHAT_MODEL=none
SENTINEL_AI_EMBEDDING_MODEL=none
SENTINEL_AI_VECTOR_STORE=none
# SENTINEL_OLLAMA_BASE_URL=http://localhost:11434
# SENTINEL_AI_MAX_OUTPUT_TOKENS=256

# Optional OTLP trace export requires the `otlp` Spring profile.
# SENTINEL_OTLP_TRACES_ENDPOINT=http://collector:4318/v1/traces
# SENTINEL_TRACE_SAMPLE_PROBABILITY=0.1
